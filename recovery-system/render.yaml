services:
  - type: web
    name: recovery-system
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node static-server.js
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
